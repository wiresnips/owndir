

module.exports = function (homestead) {

	// the idea here is, to walk down the tree, 
	// and decorate each child with an accumulated CSS attr 

	// not quite sure how it'll be applied, though - that's a bit of an exercize for later


	return homestead;
}